package homework;

public class Hw09 {
	// private < protected < (생략) < public
	// private : 해당 클래스에서만 접근 가능
	// protected : 해당 클래스를 상속받은 클래스 내에서만 접근 가능
	// (생략) : 같은 패키지 내에 있는 클래스만 접근 가능
	// public : 모두 가능
}
